# sqrt_decomposition

📊 Проект с реализациями корневой декомпозиции и наивного метода для обработки диапазонных запросов в массиве. Содержит бенчмаркинг и генерацию тестов.

---

## 🧱 Структура проекта

```
sqrt_decomposition/
├── benchmarking/
│   ├── simple.c         # Наивная реализация (бенчмарк)
│   └── sqrt_bench.c     # Реализация с корневой декомпозицией (бенчмарк)
├── realization/
│   ├── simple.c         # Чтение и обработка запросов наивно
│   └── sqrt.c           # Обработка с помощью sqrt decomposition
├── tests/
│   ├── 1.txt, 2.txt...  # Примеры тестов
│   └── generator_test.py # Генератор случайных тестов
├── CMakeLists.txt
└── README.md
```

---

## ⚙️ Сборка

```bash
mkdir build
cd build
cmake ..
make
```

---

## 🚀 Запуск

Примеры запуска из корня проекта:

```bash
./build/real_naive < tests/1.txt
./build/real_sqrt < tests/1.txt

# Сравнение производительности
./build/bench_naive < tests/large.txt
./build/bench_sqrt < tests/large.txt
```

---

## 🧪 Генерация тестов

```bash
cd tests
python3 generator_test.py > large.txt
```

---

##  Система для замеров

**Процессор:** 12th Gen Intel® Core™ i7-1255U × 12
**ОЗУ:** 15.3 GiB
**Графика:** Mesa Intel® Graphics (ADL GT2)
**Диск:** SSD 512.1 GiB
**ОС:** Ubuntu 20.04.6 LTS, GNOME 3.36.8, X11

---

##  Цель

Сравнить эффективность корневой декомпозиции по сравнению с наивным решением при обработке диапазонных запросов (например, суммы на отрезке).
